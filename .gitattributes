# Normalize text files
* text=auto eol=lf

# Shell scripts must keep LF for Linux/macOS CI
*.sh text eol=lf

# Windows command files can keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
